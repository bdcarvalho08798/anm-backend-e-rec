<%= form_with(model: document_dn_forward_department, data: { controller: 'nested-form', nested_form_wrapper_selector_value: '.nested-form-wrapper' } ) do |form| %>
  <% if document_dn_forward_department.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(document_dn_forward_department.errors.count, "error") %> prohibited this document_dn_forward_department from being saved:</h2>

      <ul>
        <% document_dn_forward_department.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


    <div class="card">
  <div class="card-header">
    Forward to Department
  </div>
  <div class="card-body">
 

  <div class="container">
  <div class="row">

    <div class="col-sm-8">
      <div class="row">
      <div class="col-sm-10">
          <template data-nested-form-target="template">
          <%= form.fields_for :document_dn_departments, DocumentDnDepartment.new, child_index: 'NEW_RECORD' do |document_dn_department_fields| %>
            <%= render "document_dn_department_form", form: document_dn_department_fields %>
          <% end %>
        </template>
        <%= form.fields_for :document_dn_departments do |document_dn_department_fields| %>
            <%= render "document_dn_department_form", form: document_dn_department_fields %>
          <% end %>
          <div data-nested-form-target="target"></div>
        </div>
          <div class="col-sm-2">
            
            <button type="button" data-action="nested-form#add"class="btn btn-sm btn-primary">Add Department</button>
          </div>
        </div>
    </div>
   
    <div class="col-sm-4">

        <div>
        <%= form.text_field :document_from_dn_id, value:@doc_id %>
        <%= form.label :description, style: "display: block" %>
        <%= form.text_area :description, class:"form-control" %>
      </div>
    </div>
  </div>
</div>

<div class="mt-2">
<%= form.submit "Forward", class:"btn btn-sm btn-success float-end"%>
</div>
  
  </div>
</div>



<% end %>
